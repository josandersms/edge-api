export const environment = {
    httpServer: {
        port: process.env['HTTP_SERVER__PORT']
    },
    kubeconfigLocation: process.env['KUBE_CONFIG_LOCATION'],
    namespace: process.env['NAMESPACE]'],
    production: true
}
